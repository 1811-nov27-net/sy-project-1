using System;
using System.Collections.Generic;
using System.Text;

namespace PizzaStore.Context
{
    public static class Mapper
    {/*
        public static Library.UserLib Map(Users users) => new Library.UserLib
        {
            Id = users.Id,
            FirstName = users.FirstName,
            LastName = users.LastName
        };

        public static Users Map(Library.UserLib users) => new Users
        {
            Id = users.Id,
            FirstName = users.FirstName,
            LastName = users.LastName
        };*/
    }
}
